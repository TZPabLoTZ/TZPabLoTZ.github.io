import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

// App  Base
  static const background = Color(0xFFF1F1F1);

// AppBar s
  static const appBarFornecedores = Color(0xFFFF7541);
  static const appBarConvidados = Color(0xFF418CFF);

// App button s
  static const buttonPrimary = Color(0xFF343434);
  static const buttonSecondary = Color(0xFFFFFFFF);
  static const buttonLogin = Color(0xFF09CFC8);
  static const buttonLoginFacebook = Color(0xFF3B5998);
  static const buttonAlertDialog = Color(0xFF81D96B);

// App text s
  static const textPrimary = Color(0xFF343434);
  static const textSecondary = Color(0xFFFFFFFF);
  static const text = Color(0xFF7F7F7F);

// App Logo s
  static const purple = Color(0xFF4D0991);
  static const orange = Color(0xFFFFB650);
  static const white = Color(0xFFFFFFFF);

// App s
  static const primary = Color(0xFF343434);
  static const accent = Color(0xFF00F0FF);
  static const primaryLight = Color(0xFF959595);
  static const blue = Color(0xFF418CFF);
  static const greyLight = Color.fromARGB(255, 230, 230, 230);
  static const border = Color(0xFF797979);

// App Status s
  static const error = Colors.red;
  static const inactive = Color(0xFFebebeb);

// AppHint s
  static const hintLogin = Color(0xFFCCCBCB);
  static const hintAlertDialog = Color(0xFFABAAAA);
}
